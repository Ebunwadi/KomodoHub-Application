<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Komodohubv2"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- School/Community Login Activity -->
        <activity android:name=".SchoolLoginActivity" />

        <!-- Developer Login Activity -->
        <activity android:name=".DeveloperLoginActivity" />

        <!-- Developer Dashboard Activity (after developer login) -->
        <activity android:name=".DeveloperDashboardActivity" />

        <!-- Role Selection Activity (Post-login/registration page) -->
        <activity android:name=".RoleSelectionActivity" />

        <!-- Principal Activity (Messaging with Admin) -->
        <activity android:name=".PrincipalActivity" />

        <!-- School Admin Activity (Consolidated features for the admin) -->
        <activity android:name=".SchoolAdminActivity" />

        <!-- Student Activities (Login and Dashboard) -->
        <activity android:name=".StudentLoginActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".EditProfileActivity" />

        <!-- Teacher Activities (Login and Dashboard) -->
        <activity android:name=".TeacherLoginActivity" />
        <activity android:name=".TeacherDashboardActivity" />


    </application>

</manifest>